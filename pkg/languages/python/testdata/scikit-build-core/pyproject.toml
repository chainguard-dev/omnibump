[build-system]
requires = ["scikit-build-core>=0.3"]
build-backend = "scikit_build_core.build"

[project]
name = "example-cpp"
version = "1.0.0"
dependencies = [
    "numpy>=1.24.0",
    "requests>=2.28.0",
]
