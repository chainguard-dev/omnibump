[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "example"
version = "1.0.0"
dependencies = [
    "requests>=2.28.0",
    "cryptography>=39.0.1",
    "urllib3>=1.26.0,<2.0",
]
